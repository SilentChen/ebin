// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("ebin", "for ebin support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("ebin", "enable ebin support", "no");

if (PHP_EBIN != "no") {
	EXTENSION("ebin", "ebin.c");
}

